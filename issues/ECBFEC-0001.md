---
title: `git pad diff`
type: enhancement
priority: P2
status: open
---

## Description

`git pad diff` much like `git diff` to show what's changed from the last commit.
With the markdown structure(frontmatter yaml and TEMPLATE), we can do a structured diff.

maybe `git pad difftool` like `git difftool` to use custom diff tool.


## Steps to Reproduce

## Expected Behavior

## Notes
